module flipper.devices.imports;

/* this module imports all the devices that are built in to
 * flipper. by importing the module, the static constructor
 * will be linked by automated build tools
 */

import flipper.devices.penguinoavr.device;